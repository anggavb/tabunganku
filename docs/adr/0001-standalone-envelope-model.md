# Standalone Envelope Model

We considered whether to require a centralized "unallocated main wallet" (zero-based budgeting) or treat each Jar as a standalone envelope. We decided that each Jar is a standalone pocket where deposits enter directly from external sources, and total savings is the sum of all Jar balances. This minimizes user friction, eliminates tedious bank reconciliation, and fits the single-user savings quest metaphor.
